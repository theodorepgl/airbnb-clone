<h1>Pages Index</h1>

<% if user_signed_in? %>
	<%= link_to 'Your Listings', listings_path %><br>
	<%= link_to 'Logout', destroy_user_session_path, data: {turbo_method: :delete} %>
<% else %>
	<%=	link_to 'Sign in', new_user_session_path %>
<% end %>
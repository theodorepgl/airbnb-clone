<h1>All Listings</h1>

<% if @listings.any? %>
	<% @listings.each do |listing| %>
		<%= link_to listing_path(listing) do %>
			<%= listing.title %>
		<% end %>
		<p>Hosted by: <%= listing.user.email %></p>
	<% end %>
<% else %>
	<p>No listings yet </p>
<% end %>